name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # 每 5 分鐘執行一次（UTC）
  workflow_dispatch:        # 支援手動觸發

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
    - name: Ping Render App
      run: |
        curl -m 60 https://linebotpipibear.onrender.com || echo "Ping failed"
